import React from 'react';
import Layout from './Layout';

const Header = () => {
    return (
        <div>
            <Layout>
                <div>돌아가기 버튼</div>
                <div>로고</div>
                <div>로그인 아이콘</div>
            </Layout>
        </div>
    );
};

export default Header;
